# Full Postgres connection string, e.g. postgresql+psycopg://user:password@host:port/dbname
# Same GCP Cloud SQL instance as the maayan_recipes project, new database within it.
DATABASE_URL=

# Optional, for the later Telegram notification feature (Phase 4). Leave blank until needed.
TELEGRAM_BOT_TOKEN=
TELEGRAM_CHAT_ID=
